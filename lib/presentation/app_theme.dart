import 'package:flutter/material.dart';

class AppTheme {
  /// main material app colour swatch
  static const MaterialColor swatch = Colors.indigo;

  /// primary and secondary app colour
  static const Color primaryColour = Color(0xff2a628f);
  static const Color secondaryColour = Color(0xff3e92cc);

  ///primary and secondary background colour
  static const Color primaryBackgroundColour = Color(0xffF5F5F6);
  static const Color secondaryBackgroundColour = Color(0xffE1E2E1);

  ///primary and secondary font colour
  static const Color primaryFontColour = Colors.white;
  static const Color secondaryFontColour = Color(0xff2E3842);
}
